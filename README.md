# LeadPad-
This extension saves LinkedIn profile information (name, title, company, location) that is already visible to the signed-in user, and exports it to CSV. It does not automate messaging, connection requests, or any interaction beyond what the user manually clicks.
